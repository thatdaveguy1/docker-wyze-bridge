# Todo

- [completed] Remove public maintainer-only operational docs from the repository.
- [completed] Keep `lessons.md` and `tasks/todo.md` as required repo files, but sanitize them so they only contain generic project-safe notes.
- [completed] Add `.github/SECURITY.md` with a private security reporting path.
- [completed] Narrow GitHub Actions permissions so only the version bump job has `contents: write`.
- [completed] Move root Python tests into a dedicated `tests/` directory and verify discovery still works.
- [completed] Apply minimal branch protection on `main` to block force-pushes and deletions without breaking direct CI-driven version bumps.
- [completed] Prepare the public `4.1.0` release surfaces: generic app naming, model-specific support matrix, Home Assistant native sidecar docs, and attribution cleanup.
- [in_progress] Diagnose the Home Assistant local OnStar MFA redirect failure after TOTP by instrumenting the patched local add-on, rebuilding it safely, and validating the real post-submit browser flow without touching production.
- [completed] Normalize the shared `frontend.py`, `site.js`, and `index.html` runtime surfaces for the local `4.2` branch while preserving intentional environment-specific port differences.
- [completed] Prepare the `4.2.0` release surfaces with aligned docs, manifests, env files, and packaging expectations.
- [completed] Run focused `4.2` validation for frontend, stream-config, talkback, and Home Assistant packaging after the final normalization pass.
- [completed] Convert `.ha_live_addon` into a distinct Home Assistant `Dev Build` add-on with its own slug for staging.
- [completed] Add SSH staging helpers for syncing the dev add-on, copying production settings, and swapping prod/dev safely.
- [completed] Add local-only maintainer docs for the Home Assistant dev-build lane, smoke checks, troubleshooting, and promotion.
- [completed] Add a fallback deployment path that reuses an already-indexed local HA add-on slot when Supervisor store reload is blocked.
- [completed] Make the Home Assistant add-on expose the standard visible login path (`Wyze email`, `Wyze password`, `Key ID`, `API key`) in both prod and dev manifests and lock it in with a packaging test.
- [completed] Clarify Home Assistant sub-stream wording so it follows the internal capability map and explicitly notes that Pan V2 is not currently included.
- [completed] Validate the current KVS startup/playback behavior in the HA dev add-on lane; no `400` warning reproduced, so no KVS runtime or logging change was applied in this pass.
- [completed] Prepare the `4.0.1` release surfaces with version bumps, changelog entries, and updated HA-facing release docs.
- [completed] Fix the HA local add-on validation lane so metadata refreshes use `ha store reload` first and dev syncs exclude macOS junk files that can break Supervisor parsing.
- [completed] Publish the `v4.0.1` Git tag and GitHub release after restoring the README homepage split between `4.0.1` patch notes and the original `4.0.0` highlights.
- [completed] Reuse and repair Scrypted Wyze MQTT motion routes so the existing `garage` device is fixed in place, `hamster` gets a single new per-camera MQTT route, and the remaining Wyze cameras are moved off software motion onto the same MQTT-backed pattern.
- [completed] Stabilize the live Home Assistant Frigate add-on after repeated crashes on Scrypted-fed RTSP rebroadcast cameras by explicitly disabling Frigate hwaccel auto-selection and using `preset-rtsp-restream` on each Scrypted RTSP input.
- [completed] Run a fully documented live Scrypted `north-yard` no-audio experiment via patched HA backups, verify that the stream became video-only during the test, and roll it back after confirming it did not resolve the underlying Scrypted/Frigate instability.
- [completed] Probe live Wyze Bridge alternate RTSP paths for `north-yard` and confirm that neither `north-yard-sub` nor `north-yardfw` is configured, which rules out the cleanest remaining Scrypted-side source swap on this HA box.
- [completed] Confirm from live bridge startup and repo code that `north-yard` is a Wyze Cam V4 (`HL_CAM4`) started as a `WebRTC source`, with no existing per-camera bridge config on this box that cleanly swaps it off the KVS/WebRTC path.
- [completed] Run a reversible live Scrypted snapshot experiment for `South Driveway E1 CX` and `Reolink Doorbell` by forcing Snapshot-plugin prebuffer snapshots through a patched HA backup restore, then roll it back after the post-restore log window failed to show a convincing improvement.
- [completed] Verify why the production Home Assistant rebuild still emits the old `WHEP_PROXY` normal-close log format after the local `whep_proxy/main.go` instrumentation patch is synced and rebuilt, then resume the `dog-run` upstream rotation investigation once runtime/source parity is confirmed.
- [completed] Fix the dev-lane `WHEP_PROXY` normal-close experiment so a `1001 Going away` signaling rotation can leave the upstream peer/media alive without collapsing the local proxy listener afterward.
- [completed] Deploy the updated normal-close experiment to the Home Assistant dev add-on lane and verify whether `dog-run` survives the next `1001 Going away` rotation window without `127.0.0.1:8080` or Frigate-side proxy failures.
- [completed] Run a longer dev-lane soak after the next `1001 Going away` rotation window and confirm `dog-run` can stay healthy well beyond the first post-rotation window with `whep_proxy` still reporting `upstream_alive=true` and `can_reuse=true`.
- [pending] Explain why the dev-lane bridge still serves existing `dog-run` traffic after the healthy `1001 Going away` rotation even when fresh probes to local `127.0.0.1:8080` and MediaMTX API ports fail from the HA shell, then decide whether that indicates namespace isolation or a latent listener/process issue.
- [completed] Add repo tooling that can query `whep_proxy` and MediaMTX from inside the bridge namespace instead of relying on HA-shell loopback probes.
- [completed] Deploy the new `/health/details` diagnostics endpoint to the Home Assistant dev lane and validate `scripts/ha_bridge_diag.sh --stream dog-run` against a live bridge session.
- [completed] Explain that the in-app MediaMTX API probe is unreachable because the add-on sets `MTX_APIADDRESS=:59997` but does not enable MediaMTX Control API (`MTX_API=true`), and update diagnostics to report that state explicitly.
- [completed] Investigate why the latest dev-lane redeploy left MediaMTX in an exit/restart loop (`Process exited with 1`) and fix the dev swap helper to wait for shared host-network ports to clear before starting the dev add-on again.
- [completed] Trim `scripts/ha_dev_build.sh` back to a minimal reliable prod/dev swap helper, document what it does and why to use it, and stop treating it as the place for stream-specific readiness experiments.
- [completed] Reproduce the current `dog-run` startup failure in the Home Assistant dev lane and confirm that downstream WHEP/RTSP can still attach before real upstream media is ready.
- [completed] Tighten WHEP proxy reuse/output gating so local tracks are not exposed as reusable output until upstream media is actually ready.
- [completed] Clear the Home Assistant dev-lane auth/init blocker where `get_user_info` returns `None` and `wyze_bridge.py` crashes before streams finish initializing, then rerun the live `dog-run` startup validation.
- [completed] Prepare the `4.0.2` release surfaces with version bumps, changelog entries, and updated user-facing release notes for the startup hardening work.
- [pending] Resume the actual `dog-run` bridge/runtime investigation by focusing on the upstream WebRTC/WHEP session path after the bridge is healthy, especially the early post-start `deadline exceeded while waiting tracks` window and later `1001 Going away` rotations.
- [completed] Verify the live March 27, 2026 routing state end-to-end: production Wyze Bridge is healthy for `north-yard` and `south-yard`, `north_yard` is still consumed by Frigate through Scrypted rebroadcast, and `south-yard` is not currently configured as a Frigate camera.
- [completed] Trace the recurring Scrypted infrastructure noise on March 27, 2026: `EHOSTUNREACH 192.168.1.217:8000` comes from an unreachable ONVIF `Back Yard`/Reolink device, while `ECONNREFUSED 127.0.0.1:38823` appears during `@scrypted/nvr` worker startup and does not stop `north-yard` decode recovery.
- [completed] Stop the crash-looping dev Wyze Bridge add-on during the March 27, 2026 investigation so shared host-network ports no longer muddy production bridge diagnostics.
- [completed] Use a fresh live Scrypted backup to recover a working admin token, then update live Scrypted so all Wyze RTSP cameras use custom MQTT motion instead of software motion, remove the stale `Back Yard`/`E1 Pro` `192.168.1.217` device, and repoint Frigate `south_yard` to the Scrypted main rebroadcast URL.
- [completed] Validate the strict March 27, 2026 Frigate routing split requested later that day: non-Wyze cameras (`south_driveway`, `north_driveway`, `doorbell`) stay on Scrypted main rebroadcast while Wyze cameras move back to direct Wyze Bridge RTSP, then prove which remaining failures are bridge-side before considering a `4.0.3` follow-up.
- [completed] Extend the dev-lane bridge so KVS-backed Wyze cameras can register `-sub` RTSP paths even when their model is outside the old firmware-gated `SUBSTREAM_FW` map, and prove that `deck-sub`, `garage-sub`, and `south-yard-sub` now come up on demand instead of staying permanently unconfigured.
- [completed] Add debug-gated `KVS_TRACE_STREAM` tracing in the dev-lane `get_camera_stream()` and `get_kvs_proxy_config()` paths so `HL_CAM4` raw stream-info and derived `/kvs-config` payloads can be compared against known-good KVS cameras without leaking tokens or ICE credentials.
- [completed] Reset the public `4.1` docs around supported behavior instead of household camera names. The README, HA docs, release notes, install/upgrade/troubleshooting guides, and a new compatibility matrix now publish per-model limits for V3, V3 Pro, V4, and Wyze Bulb Cam, describe `:19554` as the supported native Home Assistant RTSP surface, keep `:11984` internal, and rename the visible app/add-on name to `Docker Wyze Bridge`.
- [completed] Run QA audit for 4.1.1 talkback: fixed 4 bugs in go2rtc.py across all 3 runtime trees (audio_b64 UnboundLocalError, audio_b64+audio_url conflict, dead _go2rtc_ffmpeg_request function, inverted _cleanup/_talkback_temp_dir order), added 2 new tests, and brought all 11 talkback tests (test_frontend_talkback.py 4/4 + test_go2rtc_snapshot_and_diagnostics.py 7/7) to passing on macOS.
- [in_progress] Add a clean dev-only `HL_CAM4_MAIN_PROBE_MODE` switch in `.ha_live_addon` so `north-yard` main can be compared across `kvs`, `tutk_dtls`, and `tutk_parallel` without one-off code hacks, and pair it with gated `TUTK_TRACE_STREAM` logging in `wyzecam.iotc` for connect/auth timing. Focused local tests for the mode switch and trace gating now pass, and the live dev add-on comparison proved the intended branches: baseline logged `main probe mode=kvs source=kvs`, `tutk_dtls` routed `north-yard` main through `IOTC_Connect_ByUIDEx (DTLS=1)`, and `tutk_parallel` routed it through `IOTC_Connect_ByUID_Parallel`. In both TUTK main modes, `ffprobe rtsp://192.168.1.244:58554/north-yard` still ended at `404 Not Found`, so direct TUTK main did not rescue `HL_CAM4`. The remaining work is to capture why the new `TUTK_TRACE_STREAM` logger still does not emit in the live add-on process and then decide whether the next root cause is inside the TUTK connect call itself or outside the bridge code entirely.
- [in_progress] The next `TUTK_TRACE_STREAM` follow-up fixed the visibility gap by printing trace lines directly from `.ha_live_addon/app/wyzecam/iotc.py`. Live `tutk_dtls` validation on March 27 then showed the critical missing detail: `north-yard` main emits `connect_start` and reaches `Calling iotc_connect_by_uid_ex...`, but no `connect_result` ever appears before the RTSP path remains unreadable and the add-on later restarts under Wyze API rate-limit pressure. That shifts the remaining root cause from generic routing to the `HL_CAM4` TUTK connect call itself, or to behavior immediately below that SDK boundary. The next upstream-forensics pass compared `kroo/wyzecam`, `mrlt8/docker-wyze-bridge`, `IDisposable/docker-wyze-bridge`, and `AlexxIT/go2rtc`: the upstream Python bridge lineages use the same blocking TUTK SDK calls with no extra watchdog around `IOTC_Connect_ByUIDEx`, while `go2rtc`'s native Wyze path is a separate DTLS implementation with explicit 5-second handshake/auth deadlines and no `HL_CAM4`-specific workaround. The next code experiment landed in the dev-only `.ha_live_addon` lane: `HL_CAM4` TUTK connect calls are now wrapped by a local watchdog that can fire `IOTC_Connect_Stop_BySID` after `HL_CAM4_CONNECT_WATCHDOG_SECS`, and focused tests now prove both wedged DTLS and wedged parallel connects are released while fast connects stay untouched. The next step is live dev-lane validation to see whether the watchdog converts the current silent `north-yard` hang into a real timeout/error path that can retry or fail cleanly.
- [in_progress] The next `TUTK_TRACE_STREAM` follow-up fixed the visibility gap by printing trace lines directly from `.ha_live_addon/app/wyzecam/iotc.py`. Live `tutk_dtls` validation on March 27 then showed the critical missing detail: `north-yard` main emits `connect_start` and reaches `Calling iotc_connect_by_uid_ex...`, but no `connect_result` ever appears before the RTSP path remains unreadable and the add-on later restarts under Wyze API rate-limit pressure. That shifts the remaining root cause from generic routing to the `HL_CAM4` TUTK connect call itself, or to behavior immediately below that SDK boundary. The next upstream-forensics pass compared `kroo/wyzecam`, `mrlt8/docker-wyze-bridge`, `IDisposable/docker-wyze-bridge`, and `AlexxIT/go2rtc`: the upstream Python bridge lineages use the same blocking TUTK SDK calls with no extra watchdog around `IOTC_Connect_ByUIDEx`, while `go2rtc`'s native Wyze path is a separate DTLS implementation with explicit 5-second handshake/auth deadlines and no `HL_CAM4`-specific workaround. The next code experiment landed in the dev-only `.ha_live_addon` lane: `HL_CAM4` TUTK connect calls are now wrapped by a local watchdog that can fire `IOTC_Connect_Stop_BySID` after `HL_CAM4_CONNECT_WATCHDOG_SECS`, and focused tests now prove both wedged DTLS and wedged parallel connects are released while fast connects stay untouched. Live March 27 validation now confirms the watchdog does convert the old silent hang into a real SDK result on both branches: `north-yard` main `tutk_dtls` and `tutk_parallel` both return `-27 IOTC_ER_FAIL_CONNECT_SEARCH` after roughly 8 seconds, and `ffprobe` still sees `404 Not Found` for both `north-yard` and `north-yard-sub`. A follow-up dev-only retry tweak now treats watchdog-triggered `-27` as retryable; the live add-on logs show `retrying 2/3` and `retrying 3/3` for both main and substream, but the stream still never becomes readable. The next fix has to target why `HL_CAM4` search/connect itself fails, not just why the process used to hang forever.
- [in_progress] The March 28 follow-up tightened the dev-only trace without widening behavior: the existing `connect_result` event in `.ha_live_addon/app/wyzecam/iotc.py` now carries `attempt_no`, `max_retries`, and `watchdog_fired`, and focused local tests still pass. Live `tutk_parallel` retest on the dev add-on confirmed the same end state with much cleaner evidence: both `north-yard` main and `north-yard-sub` now visibly walk through `attempt_no=1..3`, both fire the watchdog at about 8 seconds, both return `-27 IOTC_ER_FAIL_CONNECT_SEARCH`, and both still leave `rtsp://192.168.1.244:58554/north-yard(-sub)` at `404 Not Found`. A same-box native `go2rtc` sidecar check was also run as outside evidence using the official `api/wyze` helper to generate `wyze://` URLs. That comparison did not rescue the diagnosis: `north-yard` (`HL_CAM4`) and even known-good `hamster` (`HL_CAM3P`) both failed in native `go2rtc` with `wyze: connect failed: discovery timeout`, so the current `go2rtc` native path is not a proven shortcut to the primary goal on this HA box.
- [in_progress] The March 28 environment follow-up proved the real dev add-on namespace is host-like, not `core-ssh`-like: a new dev-only `NETWORK_TRACE` in `.ha_live_addon/app/frontend.py` logged `hostname=local-docker-wyze-bridge-local`, outbound source IP `192.168.1.244`, default route via `enp1s0`/`192.168.1.254`, and `/etc/resolv.conf` still pointed at Supervisor DNS `172.30.32.3` with `search local.hass.io`. That means the earlier `core-ssh` `172.30.33.x` sidecar tests were not equivalent to the host-network add-on. The same pass exposed a separate contamination problem: both prod and dev currently report `started`, and the dev logs repeatedly show `listen udp :58000: bind: address already in use`, so host-network port overlap is muddying current dev-lane experiments. A first dev-only TUTK native file-log probe was added in `.ha_live_addon/app/wyzecam/iotc.py`; after fixing an initial local `c_int` vs `c_uint32` bug, the SDK call now reaches `IOTC_Set_Log_Attr` but returns `-46 IOTC_ER_INVALID_ARG` for `/config/logs/wyze-tutk-iotc.log`, so no native log file is available yet. The next step is to keep focusing on the host-network environment itself: either separate prod/dev ports or otherwise get a clean dev owner of the host ports, then continue testing whether Supervisor DNS (`172.30.32.3`) or another HA-host network quirk is what breaks Wyze discovery/search across implementations.
- [in_progress] The March 28 dev-lane isolation follow-up completed the first half of that environment work. The dev add-on now runs on its own host-network port block (`55000` web UI, `59554` RTSP, `59888` HLS, `59889` WebRTC, `59189` ICE UDP, `18080` local `whep_proxy`, and `60997` MediaMTX API), so prod and dev can stay `started` without the old `listen udp :58000: bind: address already in use` race. `scripts/deploy_ha_local_addon.sh`, `scripts/ha_dev_build.sh`, and `scripts/ha_bridge_diag.sh` now target those dev ports correctly, and the helper bug where `&network=1` was lost over SSH is fixed too. The new `network=1` diagnostics now also resolve the actual Wyze API hosts plus plausible TUTK/Kalay hostnames scraped from the SDK binary. Live HA results are clearer now: `auth-prod.api.wyze.com`, `api.wyzecam.com`, `app-core.cloud.wyze.com`, `app.wyzecam.com`, `devicemgmt-service.wyze.com`, `webrtc.api.wyze.com`, `iotcplatform.com`, `kalay.net.cn`, and `kalayservice.com` all resolve successfully inside the real host-network dev add-on. That makes Supervisor DNS look messy rather than obviously broken. The remaining main-stream blocker has snapped back into focus: `north-yard` and `south-yard` are still `640x360` on KVS main while `deck`, `garage`, and `hamster` stay high-res, so the next fix target should return to KVS profile/selection behavior instead of generic HA DNS failure.
- [completed] March 28, 2026 full e2e validation. All services healthy: prod bridge V4.0.2 serving 6 active streams (5 Wyze + dog-run offline), dev bridge on separate ports, Frigate serving all 8 cameras with healthy FPS and HTTP 200 snapshots, Scrypted MQTT motion verified end-to-end on all 5 Wyze cameras (garage, deck, south-yard, north-yard, hamster) with HKSV recordings triggering from MQTT motion events. Routing confirmed: Wyze cameras on direct bridge RTSP (:58554), non-Wyze on Scrypted rebroadcast. Known limitations: north-yard (HL_CAM4) and south-yard (HL_BC) locked at 640x360 by Wyze firmware/KVS — not fixable from bridge side. Stale `-sub` WHEP proxy retry noise present but harmless.
- [completed] March 28, 2026 native go2rtc follow-up for north-yard and south-yard. Dev sidecar packaging/build drift was fixed and the native go2rtc sidecar was brought up cleanly on a dedicated RTSP port (`:19554`) with a live API on `:11984`. Persistent `north-yard`, `north-yard-sd`, `south-yard`, and `south-yard-sd` aliases can be seeded into go2rtc config, but all four native RTSP paths still fail live with `404 Not Found`, while the production bridge continues to expose only `640x360` main feeds and both `-sub` bridge paths return `400 Bad Request`. Current conclusion: on this HA box, neither the primary goal (main/sub for north and south) nor the fallback goal (single 2K+ feed for each) is currently achievable from bridge-side or native go2rtc-side changes alone.
- [completed] March 29, 2026 north-yard native go2rtc breakthrough. The dev add-on now preserves a seeded `/config/go2rtc_wyze.yaml` across restarts, and live `go2rtc` API seeding for `north-yard`/`north-yard-sd` is proven on-box at `:11984` API and `:19554` RTSP. After restart, `ffprobe` and `frame.jpeg` both confirm `north-yard` main at `2560x1440` and `north-yard-sd` at `640x360`. Frigate `north_yard` was cut over to those dev go2rtc paths (`record` on main, `detect` on sub), Frigate restarted cleanly, and the internal Frigate snapshot endpoint returned HTTP 200 again.
- [completed] Repeat the same native go2rtc validation for `south-yard` on the dev add-on. Live `go2rtc` aliases for `south-yard` and `south-yard-sd` now persist in the dev sidecar config and both probe cleanly, but both RTSP and `frame.jpeg` still resolve to `640x360`. So `HL_BC` does not gain a meaningful main/sub split or a better single feed from the native go2rtc path on this box.
- [completed] March 29, 2026 post-`4.1.1` HA SSH retest on the local dev add-on lane (`local_docker_wyze_bridge_local`). The current `.ha_live_addon` tree was synced and rebuilt successfully over SSH, then re-validated in a clean dev-only window. `south-yard` still reported bridge-first selection in `/health/details`, and fresh RTSP probes confirmed no improvement for the Wyze Bulb Cam path on this box: `rtsp://homeassistant.local:59554/south-yard`, `rtsp://homeassistant.local:19554/south-yard`, and `rtsp://homeassistant.local:19554/south-yard-sd` all resolved to `640x360`, while `south-yard-sub` on the bridge still returned `400 Bad Request`. Current conclusion remains unchanged: there is no validated Gwell/native improvement for South Yard here.
- [completed] Mirror the proven March 29, 2026 North Yard native `go2rtc` design into the production source tree only. `home_assistant/app/run`, `home_assistant/Dockerfile`, and `home_assistant/config.yml` now carry the North-only sidecar pattern on API `:11984` and RTSP `:19554`, while `south-yard` is intentionally left on the existing bridge path until it proves a real gain. Packaging coverage now also asserts the production source tree exposes the native RTSP port and downloads the `go2rtc` binary, but no live production add-on rollout was attempted from this repo-only mirror.
- [completed] Run a March 29, 2026 South Yard dual-stream option sweep in the Home Assistant dev lane. All six executable ideas were tested end-to-end with rebuild/restart plus live RTSP probes. Result: none changed the live `HL_BC` outcome on this HA box. Native `go2rtc` `south-yard`/`south-yard-sd` stayed `640x360`; forced main-stream TUTK accepted the connect/auth path but never surfaced a stable readable RTSP stream; `PRO_CAMS += HL_BC` still left KVS main at `640x360`; alternate `QUALITY` strings (`HD240`, `HD60`, `SD120`, `HD`) were all accepted by Supervisor but still served `640x360`; and KVS signaling URL parameter injection only caused websocket `bad handshake` retries before returning to the same `640x360` output. The dev lane was restored to the known-good baseline afterward.
- [completed] Close the remaining March 29, 2026 South Yard gap checks in the Home Assistant dev lane. `HL_BC` main `tutk_parallel` was validated live and failed immediately with `IOTC_ER_DEVICE_REJECT_BY_WRONG_AUTH_KEY (-68)`, so it is not a useful alternative to `tutk_dtls`. A combined `PRO_CAMS += HL_BC` plus native `go2rtc` retest was also run and still left `south-yard` and `south-yard-sd` at `640x360`. The dev lane was restored to baseline again after both follow-ups.

- [completed] Start the `4.1.1` runtime/bootstrap pass by moving the native `go2rtc` sidecar startup into a dedicated helper, wiring all three `app/run` entrypoints through it, and bundling the `go2rtc` binary in the root Docker images so non-HA containers can exercise the same bootstrap path.
- [completed] After the shared bootstrap slice is verified, extend the Python runtime with camera-level native selection metadata, snapshot routing, diagnostics, and API-first native talkback so `HL_CAM4` can auto-prefer native while `HL_BC` stays bridge-first unless a real gain is proven.
- [completed] Run the remaining `4.1.1` HA live validation pass for native selection, snapshots, diagnostics, and talkback on the real validation host. March 29, 2026 live validation now covers `north-yard` native selection plus native snapshot, `south-yard` bridge-first diagnostics, and successful API-first uploaded-audio talkback on `north-yard` through the native `go2rtc` alias.
- [completed] Clean up the Home Assistant settings UX so required login fields stay at the top, optional help text is clearer, and per-camera stream mode can be chosen as `Main`, `Both`, or `Sub` from both `CAM_OPTIONS` and the Web UI, with unsupported `Sub` disabled in the UI. March 29, 2026 live follow-up: the active dev add-on at `local_docker_wyze_bridge_local` was synced and rebuilt successfully, remote manifests/docs now expose `CAM_OPTIONS[].STREAM`, the live Web UI on `:55000` rendered stream-mode controls for all visible camera cards, and a real `deck` mode round-trip (`main -> both -> main`) succeeded through `/api/deck/stream-mode`. On this HA box, every visible camera currently reports `supports_substream: true`, so the disabled-`Sub` state was verified in code/tests rather than with a live unsupported camera card.
- [completed] Replace the coarse per-camera `Main/Both/Sub` settings UX with granular feed selection. The runtime, API, Home Assistant schema, and Web UI now model per-camera `HD` and `SD` feeds independently, allow separate kbps targets for each feed, surface reported feed resolutions on the camera card, and disable unsupported feed toggles so Bulb Cam style `SD-only` cameras no longer pretend an HD path exists.
- [completed] Deploy the granular HD/SD feed-selection UI to the live Home Assistant dev add-on and browser-test it in WebKit. March 29, 2026 live result: `local_docker_wyze_bridge_local` on `http://homeassistant.local:55000` now serves the new `stream-config` UI, `south-yard` shows `HD` disabled with `HD stream is not available for this camera`, `SD` enabled with `640x360`, `deck` completed a browser-driven `main -> both(sd60) -> main(sd30)` round-trip, and a fresh WebKit pass ended with `0` console errors.
- [completed] Massively simplify the Home Assistant add-on configuration page by removing rarely used power-user fields from the HA form, reordering the remaining settings around the real setup flow, adding nested `CAM_OPTIONS` labels/descriptions, and validating the result end-to-end in the dev add-on lane. March 29, 2026 live result: the dev slot source and loaded Supervisor schema now match the trimmed manifest, the top live field order is `WYZE_EMAIL`, `WYZE_PASSWORD`, `API_ID`, `API_KEY`, `TOTP_KEY`, then the common streaming defaults, nested `CAM_OPTIONS` labels now load through Supervisor translations, and the dev add-on is healthy again on `http://homeassistant.local:55000` and `http://192.168.1.244:55000` after a forced uninstall/reinstall plus settings restore.
- [completed] Run the second-pass Home Assistant config cleanup requested after the first live screenshot review. March 29, 2026 live result: `MEDIAMTX` is no longer present in the loaded dev-lane schema, the renamed labels now read `Preferred video quality`, `Preferred lower-bandwidth quality`, `How cameras connect`, `Audio format`, and `Per-camera settings`, and the live dev add-on options now carry a trimmed `API_ID` without the old leading space.
- [completed] Fix the `4.2.1` Home Assistant feed-default bug where explicit per-camera `CAM_OPTIONS` `HD` and `SD` values did not take effect unless `/config/wyze_camera_settings.json` also existed. `camera_feed_config()` now honors explicit env-backed `HD_*` and `SD_*` defaults before legacy `STREAM` fallback, focused regression coverage passes, the dev add-on was validated live without a runtime settings file, and production was restored afterward with the intended `SD`-only camera defaults.
